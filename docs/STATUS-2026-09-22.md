# Overnight build status — 2026-09-22

*Written by Claude Code for Faraz after the autonomous run authorised on the evening of 2026-09-21 ("continue with all
the prompts you can finish tonight; latitude to optimize and adjust"). Everything below was verified by observing
behaviour (tests, live rows, CI runs, screenshots), never assumed. The milestone schedule was generated as a preview
report and is NOT published: you review it and publish from the app.*

## 1. Where things stand

| Prompt | Status | Where to look |
|---|---|---|
| 0A repo + Pages | done | https://github.com/fkhan628/Silvis-Call-Schedule, https://fkhan628.github.io/Silvis-Call-Schedule/ |
| 0B orientation | done | `docs/ORIENTATION-2026-09-21.md` (reuse map, 715-item blast radius, defaults taken, your decisions) |
| 1 scaffold | done | first CI run proved the pipeline (bot commits `index.html` with `[skip ci]`) |
| 2 schema + RLS | done, applied live | `docs/SCHEMA-REVIEW.md`, `scripts/verify-rls.sh` (6/6), `docs/ONBOARDING.md` |
| 3 rules engine | done | `rules.js`, `test/rules.test.js` (622 assertions) |
| 4 generator | done | `generator.js`, `test/generator-regression.js` (~194k assertions in ~5 s) |
| 5 seed import | done, applied live | `importer.js`, `scripts/import-seed.js` (idempotent; re-run = no changes) |
| 6 UI slices A–G | done | calendar, week rows, day editor, Setup, Totals, My Schedule, Time off & Trades |
| 7 East feed + forecast | done | `east-feed.js`, `scripts/east-forecast.js`, `docs/east-forecast-latest.json` |
| 8 generate | preview done, **not published** | `docs/PREVIEW-2026-11-02-to-2027-01-03.md` |
| 9 exports | done | ICS, share page, printable month, ER Call Panels + Copy for Word |
| 10 edge functions | retargeted and deployed; secrets/cron are yours | `edge-functions/README.md`, `docs/EDGE-FUNCTIONS-REVIEW.md` |
| 11 hardening | see section 6 | |

Twenty substantive commits on `main` tonight, each with its own adversarial review round (findings fixed and re-verified
before commit). CI runs eight test suites before every build.

## 2. What is live right now

- **App** at the Pages URL: daily-model shell, login gate, month grid with the ER-panel author's week rows, eligibility-aware day
  editor, full Setup (roster, Users, rules editor with pattern preview, availability with paste-a-date-list, vacations,
  holidays, East feed panel, Generate with preview → Accept & Publish, Import seed, office contacts), Totals (no dollar
  figures anywhere), My Schedule, self-service Time off, Trades by day + role, Settings and data management.
- **Database** (`bzhsroegtagqhutbnsrp`): schema applied; anon can read the six public tables and nothing else; anon
  writes are refused; the vacation trigger refuses a range over a published day and the day before a primary day. Seed
  imported: 53 schedule days (Sep 14 – Nov 1 plus your Thanksgiving Thu–Sun), 36 availability statements, 3 vacations,
  the roster/rules/holidays blob (no contact data anywhere). East cache: 7 published Davenport weeks + 14 forecast weeks.
- **Edge functions** deployed with JWT verification off: `calendar-sync` already serves feeds (`?surgeon=FAK` → 4
  events, group → 78), `office-notifications`, `send-notification`, `daily-reminder` answer 401 without their secret
  or a session and cannot send mail until you set `RESEND_API_KEY`, `NOTIFICATION_FROM_EMAIL`, `CRON_SECRET`.

Verification numbers (local run at the end of the night):

| Suite | Result |
|---|---|
| rules | 622 assertions |
| east-feed | 68 |
| data-layer contracts (safeguards, CAS pins, weekly-model absence) | 56 |
| importer | 309 |
| week rows | 10 |
| exports | 33 |
| totals | 23 |
| generator regression (50 seeds × 3 ranges + bestOf 200) | 194,244 |
| Playwright smoke harness (mocked auth, live data, 390 px, dark mode) | 172 checks |

## 3. The milestone preview (Prompt 8) — waiting for you

`docs/PREVIEW-2026-11-02-to-2027-01-03.md` (seed 7, bestOf 200, live rows, real East feed + forecast):

| Result | Value |
|---|---|
| Open primary days | 0 |
| Open backup days | 3 — Thu 11/05, Thu 11/19, Thu 12/03 |
| Hard-rule violations | 0 |
| Soft penalty sum | 122 |

Why the three Thursdays are open: you never take Tue/Thu (OR days); Burchett's whitelist has no Thursday; Acton is the
primary those days (on vacation 11/19); Philip's week is not on his list; Fierce is in Clinton; Sarkar is outside her
window. **Cheapest lever:** allow you as *backup* on Tue/Thu (`surgeonRules.s1.hardNeverWeekdaysRoles = ["primary"]`
in Setup → Rules).

Things in the preview you may want to change before publishing — all data levers in Setup → Rules, no code:

- **Khan is mostly weekend backup** (9 primary / 21 backup across the range, 22 weekend days, back-to-back weekends):
  you have no target or cap while everyone else is capped or windowed. A `monthlyTarget` or a weekend cap for `s1`
  rebalances it.
- **Acton absorbs December** (16 days) — uncapped by your decision; a cap or target for `s3` changes that.
- **Burchett 12/30 → 1/1 is three straight primary days** because a holiday unit counts as one commitment
  (`groupRules.holidays.unitExemptFromMaxConsecutive = true`, chosen so your 4-day Thanksgiving unit passes your max
  of 3). Set it to `false` to keep units but forbid the day before.
- **Philip is capped at 8 total per month** (group default; he never stated a cap; his own October was 15). Set
  `surgeonRules.s4.monthlyCap` if he should carry more.
- **Khan on 12/18–20 primary** carries a 19 % East forecast; the forecast says Davenport gives you the 12/21 service
  week and Christmas 12/25–26 with near certainty, and the generator kept you off Silvis primary those days.

## 4. What only you can do

1. **Sign up** in the app, then promote yourself (SQL in `docs/ONBOARDING.md`), then invite the others from the private
   contacts file (Supabase dashboard → Authentication → Users → Invite). The database creates their profile rows; link
   each to a roster id and role in Setup → Users.
2. **Supabase Auth URL settings:** Site URL and Redirect URL = `https://fkhan628.github.io/Silvis-Call-Schedule/`
   (otherwise invite and password-reset links do not land in the app).
3. **Secrets + cron** for the mail functions (`edge-functions/README.md`), then only the safe verifications listed there;
   the live `publish`, `digest`, `test` and reminder invocations send real mail.
4. **Review the preview and publish** from Setup → Generate (Accept & Publish snapshots first, confirms before touching
   any locked slot, then shows the office-notification diff).
5. **Davenport side:** block 11/26–29 for yourself there; when you generate DSG at year end, enter your published Silvis
   days as constraints, then refresh the East feed in Setup.

## 5. Deviations from the prompts (deliberate, reversible, all reviewed)

- `index-source.html` was rebuilt on the daily model rather than trimmed line by line, per your "bones and safety
  features, not a verbatim translation"; every safety contract (wipe guards, snapshot-before-destructive with capture
  failure blocking, per-day compare-and-swap, failed-read ≠ empty) is re-pinned by `test/data-layer.test.js`.
- The generator regression loops run at bestOf 6/5/2 (plus one bestOf 200 run) to respect Prompt 4's 10-second budget;
  eligibility and weekend-pattern enumeration dominate the profile.
- Forecast rows live in their own table (`east_forecast`), invisible to the published-week derivers by construction.
- Philip's `availableWeeks` and Sarkar's windows are read from the rules blob, never written as availability rows (a
  dated row lifts weekday patterns, which would have opened Sarkar's Fridays).
- `apply_trade()` (SQL, definer) applies accepted trades because members cannot write `schedule_days` under RLS; a
  holiday unit or weekend block trades as a whole.
- The importer scrubs personal wording from notes that land in anon-readable tables and never overwrites a day the app
  has edited or filled.
- Contact data: none in the repo, seed, docs, schema, config or any anon-readable table (grep gate enforced; the
  `@example.com` input placeholders are the only address-shaped strings).

## 6. Hardening (Prompt 11)

- **No silent failures:** 37 silent catches and unchecked fetches inventoried and fixed — every catch warns, every
  user-initiated failure toasts; the keepalive day/blob flushes re-arm on a server rejection instead of assuming
  success; the periodic snapshot's result is inspected; a corrupt config blob now *blocks* destructive actions instead
  of reading as "nothing to snapshot"; a network error during token refresh no longer logs you out silently.
- **Audit log:** every listed action writes a row (`schedule.publish`, `schedule.generate_accept`, `schedule.day_edit`,
  `schedule.lock`, `seed.import`, `timeoff.add/remove/edit`, `trade.propose/accept/decline/cancel` (+ `trade.apply`
  from the SQL function), `snapshot.restore`, `data.reset`, `data.import`, `users.link`, `rules.edit`,
  `availability.add/remove`, `east.refresh`, `east.override`); outcome-bearing actions log *after* the write with
  `outcome` so an aborted reset never claims it happened.
- **Phone layout (390 px):** no sideways page scroll; wide tables scroll inside their card with a swipe hint; tap
  targets ≥ 36 px; sticky Save in the day editor; trade form and Generate score readable.
- **Refresh mechanism:** harness scenarios prove both banners (newer `version.json`; `client_versions.main.min_version`
  above the running build) and that Reload calls the hard reset.
- **Data management end to end (harness, recorded writes):** backup export = `{ config, schedule_days, time_off,
  availability }` matching the live counts; malformed imports refused before any write; factory reset requires typing
  `RESET`, records the snapshot *before* the delete and aborts with zero writes when the snapshot fails; restore
  replays config → day CAS writes → table upserts and the restored map byte-compares equal to the snapshot.
- **Coverage at a glance:** a strip above the calendar (next 60 days: open primary / open backup with the first day,
  forecast-busy days assigned as primary, East-feed coverage end, last published, last snapshot age), each count
  opening the first such day.
- Harness now 207 checks; the re-audit left six minor notes (dark-mode shade on the scroll hint, unconditional swipe
  caption, password flows' error surfacing, crash-screen reload, snapshot-age wording after a skipped capture, audit
  insert failures console-only). Test fixtures use only reserved fake domains (`example.com`, `example.test`) and a
  `555` number; the contact-data gate exempts those.

## 7. Open questions

1. 10/15 primary — imported open; the generator lists it with per-surgeon reasons.
2. Sarkar's home email — private file only.
3. Holiday unit days for 2027 and the minor holidays.
4. Khan as backup on ordinary Tue/Thu (the lever above).
5. Philip's monthly cap.
