# Status for Faraz - morning of 2026-09-23

*Written overnight by Claude Code under your 9/22 evening authorisation ("deploy, publish and move forward without my
go - just ensure it is accurate to the best of your ability; no one has access yet except me"). Everything below was
verified by observation (a live row, a CI run, a served file, a probe result); nothing was assumed. Committed to the repo
on 9/23 (audit F01 / LVD-5); the PENDING marks of the overnight draft are resolved in section 3.*

## 1. What is live right now

| Thing | State | How it was verified |
|---|---|---|
| App on GitHub Pages | build **2026.09.23c** (main `875bbcd`): every Prompt 12 item incl. the 9/22 evening rules, the Illini navy / orange theme with the SSC icons, the open-shifts board (Prompt 13), the review fixes RF1-RF3 and the importer fix (a seed re-import keeps published / app-edited days instead of refusing) | `version.json` served `2026.09.23c`; manifest `#FF5F05`; sign-in screen loaded in a browser at 04:45 with no console errors; CI run 35843609482 green |
| Published schedule | **2026-10-07 -> 2027-01-03** written into `schedule_days` (70 rows: 39 inserts, 31 updates; 110 OPEN -> holder fills; nothing replaced or cleared); 112 rows in the table; the only open slot in range is **Thu 10/15 primary** (nobody's rules allow it - open question 1) | `scripts/publish-preview.js --apply`: snapshot `159b0dfe...` first (7 -> 8), audit `schedule.generate_accept` `55a5ed18...`, re-read verify, fresh plan = 0 rows; independent SQL re-read: 112 rows, one open slot, 8 snapshots, 0 notifications written |
| Seed / blob | equals the repo's `docs/silvis-seed.json` at the merged head (17-change import at 21:51, 8-change import at 00:27, blob-only import at 04:27: the retired `seedRevisions` prose left the anon blob, `seedCoreHash` stamped, roster s6 fullName blank) | importer dry run reads `Total changes: 0 (+32 blocked)` - the 31 published days are listed as kept, never touched |
| Prompt 14 schema | `call_offers`, `call_periods` (+ `offer_modes` column tonight), `offer_status()`, triggers OF001-003, RLS as reviewed; `claim_open_slot` now also records a claim as an offer | rolled-back probes green (cases A-K and the claim probe A-L); no rows left behind |
| Prompt 15 schema | `east_vacation_reviews` (the away / home decision per mirrored East vacation range; authenticated-read, write own rows or scheduler) created 04:37 - EMPTY until you review a range | 4 policies all `{authenticated}`, RLS on, rolled-back probe A1-J exactly as specified, 0 leftovers; anon read is a silent `[]` |
| East vacations app (Prompt 15 parts 1-4) | deployed on main at 09:35 (build after 2026.09.23e): the East feed now also mirrors your Davenport vacations into `east_feed`, unreviewed / away ranges are derived vacations for the generator and the editors, home ranges lift the Tue/Thu OR-day rule; Setup -> East feed and your Time off view carry the away / home choice | 6 commits reviewed adversarially, smoke OK 319/0 on the final tree, all suites green; **your step: Setup -> East feed -> Refresh from Davenport once**, then decide 11/25-11/29 and 12/11-12/13 |
| Nine backup reassignments (your 9/23 morning list) | applied 08:38 with the reviewed command-line day editor: 10/9, 10/15, 10/20, 10/22 -> Burchett; 11/19 -> Philip; 11/24 -> Khan; 12/4 -> Fierce; 12/10 -> Khan; 12/18 -> Acton - all locked, source manual, primaries untouched, 10/15 primary still open | snapshot `bed7558b...` first (snapshots 12), 16 audit rows, re-read of the nine rows, 112 rows total, 0 notifications; before / after table in docs/REPORT-BURCHETT-OCTOBER-2026-09-23.md; Burchett four October backup-only availability rows applied at 08:55 (availability 48 -> 52, snapshot 13) |
| Water-filled share (your 9/23 spec decision) | deployed on main at 10:05 (build after 2026.09.23h) and the seed knob `groupRules.weights.deviationConvexity = 2` is in the live blob (snapshot 14): a surgeon's locked and derived-week days count against the equal share of ALL the month's slots, no locked floor, convex deviation above target, both roles; the Generate panel and Totals titles say so. **The published schedule was not regenerated.** What-if over the exact publish inputs: November backups would have been Khan 5 / Burchett 4 / Acton 4 / Philip 5 / Fierce 11 / Sarkar 1 (published 3 / 4 / 3 / 4 / 16 / 0); 34 of 224 slots differ; 10/15 primary still the only open slot; soft-penalty sum 70 -> 96 (the knob is the lever: 1.5 -> Fierce 13 / 84). Full tables in docs/REPORT-WATER-FILL-2026-09-23.md | test-first (synthetic tally test fails on the old generator, passes on the new; two mutants fail), a November 2026 fixture case in the chain, 15 suites green, smoke OK apart from the two import pins that cleared with the blob apply; two reviewers re-derived the what-if |
| Notifications | **none sent** all night; the publish notice to the group is yours to send from the app's publish dialog when you invite people | `notifications` count unchanged in the last hours |

## 2. What you should do this morning (in this order)

1. Open the app (hard-refresh once; installed PWAs pick the new icon up on the next manifest refresh - iOS may need
   remove + re-add). Check the calendar: October backups filled from 10/7, November as the ER-panel author published plus the
   generated fills, December and 1/1-1/3 generated. Thu 10/15 primary is OPEN on purpose.
2. Setup -> Generate now defaults to the first open slot from today (10/15 primary today). **Do not regenerate** unless
   you want a different draw: generated days are unlocked, so a new Generate + Accept & Publish would rewrite them
   (locks, claims and trades are never touched; your nine hand edits of 9/23 are locked). A "fill open slots only" control
   was added for later gap-filling.
2b. Setup -> East feed -> **Refresh from Davenport** once (this pulls your Davenport vacations into the cache), then mark
   the ranges away or home in the same panel.
3. Invite the surgeons and the ER-panel author (docs/ONBOARDING.md; Setup -> Users). Sarkar's e-mail is still missing from your
   private contacts file.
4. Send the publish notice from the publish dialog when you are ready (it e-mails only users who have signed up).
5. Read section 4 (decisions only you can make) and answer at your convenience.

## 3. What changed tonight, item by item (all on `main` unless noted)

- **Prompt 12 (all items):** I, D, F, P, Q, A, K, S, N (revised), J, L, T, C, M, B, H, small items, U, V, W, X, Y, Z,
  AA, AB, AC, the theme (O.1-O.3 + R). Full list with tests in docs/PROMPT-12-PREPUBLISH-FIXES.md. The regenerated
  preview and its diff are docs/PREVIEW-2026-11-02-to-2027-01-03.md / docs/PREVIEW-DIFF-2026-09-22.md; the publish
  report is docs/PUBLISH-2026-09-23.md.
- **Prompt 13 (open shifts):** rebased onto the Prompt 12 head and deployed. Claims and trades are now FIXED slots
  for the generator (a claim is never silently regenerated). Edge functions unchanged (v3 still valid).
- **Whole-branch adversarial review:** 7 lenses, 55 confirmed findings, 0 blocking; the fix groups RF1 (CI
  hygiene), RF2 (app safety: confirm before replacing an unlocked held assignment on Accept & Publish, keepalive
  flush during a long publish, fill-open-only control, importer refuses to overwrite an app-edited blob, revision
  prose kept out of the anon blob) and RF3 (docs/seed staleness) are all deployed in build c, plus the importer
  fix (published / app-edited days are kept on a re-import; `--strict-blocked` restores the refusal).
- **Prompt 14 (offers):** parts 1 (schema artefacts), 2 (offers-first rules + generator, claim-as-offer), 5 (first
  period + importer conversion of the dated lists - NOT applied live yet, see section 5), 4 (offers reminders /
  close in the edge functions + cron text - NOT deployed yet), 6 (docs) are committed on `feat/offers`, each
  reviewed adversarially; part 3 (the phone painter + Periods UI) - also on `feat/offers` as of 9/23. Nothing of
  Prompt 14 is merged or deployed except the schema of part 1 (section 1).
- **Prompt 15 (East vacations):** the Davenport `time_off` table is read through the East feed's existing read path (path 1a);
  parts 1-4 merged to `main` at `19b9efc` and deployed (build `2026.09.23g` and later; section 1).

## 4. Decisions only you can make (nothing here blocks the rollout)

*Morning updates (9/23):* Burchett's December list now governs BOTH roles (seed + live blob + build d); the three published
backups it invalidates are 12/4, 12/10, 12/18 - eligible replacements in docs/REPORT-BURCHETT-DECEMBER-2026-09-23.md; you asked
for 12/4 -> Fierce, 12/10 -> Khan, 12/18 -> Acton and 10/9, 10/15, 10/20, 10/22 backup -> Burchett (his 9/23 note), applied
as locked manual edits with the reviewed day-edit tool (see the addendum at the end once done). docs/PROMPT-12-PREPUBLISH-FIXES.md
is deduplicated and guide section 18 (East vacations) is restored.

1. **10/15 primary** stays open: nobody's rules allow a Thursday there (Philip is on vacation). Assign by hand, or
   ask the group.
2. **Philip carries the Tue/Thu gap:** sole eligible primary on 11/10 and 11/24 (November) and on Tue 12/22 and 12/29
   once Acton is off Tuesdays. Relief = an Acton dated row / offer for a specific Tuesday (own dates beat own
   patterns), a Khan date with no East OR block, or an outside surgeon.
3. **Burchett and holiday units:** explicit dated lists are never waived on holiday units (small items), so Burchett
   is out of the Thanksgiving backup pool (Philip / Fierce remain) and out of the Christmas unit (12/24 is not on his
   December list). A dated row for him restores it.
4. **Offers modes for the first period** (Prompt 14 part 5 default): Burchett and Philip exhaustive ("only these
   days"), Acton and Fierce preferred, Khan and Sarkar rules-only. A claim by a not-started surgeon makes him
   "submitted / preferred" for the period (rules-only claimers write no offer row) - confirm.
5. **DECIDED 9/23 (TRIM):** the public repo carries rules only. Acton's and Philip's lines are reworded, the reason words
   are swept from docs/ and the seed (notes read "off (stated 9/17)"), section 8 item 16 is marked decided; the reasons live in
   your private notes file in the OneDrive folder. Live blob updated (settings stamp only), build 2026.09.23e.
6. **Thanksgiving 2027 = Thu-Sun and July 4 2027 = Sat 7/3 - Mon 7/5** are in the seed; a Saturday July 4 (next 2037)
   is the builder's Friday-alone default - undecided.
7. **Standing East days and a one-year exception:** a manual day-editor edit with the visible warning is the path; a
   per-year exceptions field was not added.
8. **Setup edits vs re-import:** a seed re-import replaces rules / roster (pool) / holidays wholesale; RF2 makes the
   importer refuse when the blob was edited in the app unless told to overwrite. Until then, mirror Setup edits into
   the seed before any re-import.
9. **East forecast is the 100-run build of 9/22.** Regenerate at 200 runs on your machine when convenient
   (`node scripts/east-forecast.js --sql`, then paste the printed SQL).

10. **Home week vs weekend block (new, from the water-filled share):** when an East "home" week hands you weekday primaries,
    your primary share for the month is met and the convex term no longer gives you a full weekend block that month. The
    regression accepts that for now; whether a home week should still come with a weekend block (data: weights.weekendContribution)
    is your call. Rules doc section 8 item 18.

## 4b. Whole-system review (9/23, after your morning items)

Six independent lenses over the deployed app and the live tables raised 47 findings; 40 survived two adversarial verifiers each,
none blocking, seven major. Fixes run in four lanes and land through the day: notifications function role gate (any verified
session could broadcast - fixed before anyone else has access), restore / import partial-failure handling, stale "open" items in
the rules doc after the publish, ONBOARDING's promise of a Monday e-mail whose cron job does not exist yet, guide section 17's
status line, a committed session path in the smoke harness, a past-day guard for apply_trade, dead seed keys. The confirmed
list with evidence is in the session scratch (audit/confirmed.json) and each fix commit names its finding ids.

## 5. Live steps that remain for you (only if you want them)

- Cron jobs: `silvis-offers-daily` (Prompt 14 part 4) - the statement is in edge-functions/README.md section 4 once
  part 4 lands; the two Prompt 13 jobs were pasted on 9/22 per the README.
- Nothing else: every schema, function and data step of the night was applied and probed by me.

## 6. Where to look

- Rules of record: docs/SILVIS-CALL-RULES.md (every "9/22" and "9/22 evening" mark).
- How it is built: docs/SILVIS-BUILD-GUIDE.md sections 15-19.
- Publish / import tools: `scripts/publish-preview.js` (dry run by default), `scripts/import-seed.js` (dry run by
  default; `--apply --workdir <linked dir>`).
- Snapshots: Setup -> Data management -> restore (8 snapshots tonight; the publish snapshot is `generate_publish`).
