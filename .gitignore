# Silvis-Call-Schedule
# NOTE: index.html is deliberately NOT ignored — CI builds it and commits it back.
node_modules/
.DS_Store
*.log
.env*
# Contact data never enters the repo (guide §3.1); the only contact file lives in OneDrive.
silvis-contacts.md
# package-lock.json IS tracked (Prompt 16 B8): CI installs with `npm ci` against it.
# Playwright smoke-harness screenshots (test/ui/smoke.mjs)
test/ui/out/
# Local pre-deploy backups of the live edge-function sources (supabase functions download)
edge-functions/deployed-backup-*/
