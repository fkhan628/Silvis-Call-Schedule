{
  "_meta": {
    "purpose": "Regression case for Prompt 13 / P13R (2026-09-23), generator GEN_PERSON_FIXED_SOURCES: two slots of the locked October import are held WITHOUT a lock flag by a surgeon acting for himself - Khan as backup on Wed 2026-10-21 through claim_open_slot (row source 'claim') and Acton as primary on Sat 2026-10-24 through apply_trade (row source 'trade'; off his governed October list, and his 10/23 + 10/24 make two straight primaries under his max 3). Neither SQL path sets a lock flag. Both a plain generate() and generate(..., { fillOpenOnly: true }) treat every held role of such a day as FIXED - byte-identical on the output, lock flags untouched, counted in diagnostics.fixedSlots, never a hard violation (conflicts go to diagnostics.fixedViolations) - and fill only the open slots around them. The sibling fixture fill-open-only-2026-10.json pins that the same two slots with source 'manual' ARE regenerated by a plain generate (the day editor has its own lock toggle).",
    "shape": "schedule fragment merged over the seed's existingAssignments before rules.buildContext (guide section 4.1); each entry replaces the seed's row for that day",
    "range": { "start": "2026-10-15", "end": "2026-11-01" },
    "contact_data": "none"
  },
  "schedule": {
    "2026-10-21": {
      "primary": "s3",
      "backup": "s1",
      "primaryLocked": true,
      "backupLocked": false,
      "source": "claim",
      "externalCover": null,
      "note": "fixture: Acton primary as imported (locked); Khan took the open backup through claim_open_slot (no lock flag)"
    },
    "2026-10-24": {
      "primary": "s3",
      "backup": null,
      "primaryLocked": false,
      "backupLocked": false,
      "source": "trade",
      "externalCover": null,
      "note": "fixture: Acton holds the primary through apply_trade (no lock flag; off his October list; 10/23 + 10/24 = 2 straight)"
    }
  }
}
